# Session Note

Date: 2026-06-11

## Objective

Create a new VSIX package set by promoting the extension version to `0.6.0`.

## Scope

- update the extension manifest version
- align current release-facing package references
- rebuild the target-specific VSIX artifacts

## Work Completed

- Bumped `vscode-extension/package.json` from `0.5.0` to `0.6.0`.
- Updated current release references in the root README, extension README, and roadmap to match the new package version.
- Rebuilt the full target-specific VSIX set with `cd vscode-extension && npm run package:all`.

## Validation Results

- Passed:
  - `cd vscode-extension && npm run package:all`
- Produced:
  - `vscode-extension/pbir-design-analyzer-0.6.0-win32-x64.vsix`
  - `vscode-extension/pbir-design-analyzer-0.6.0-win32-arm64.vsix`
  - `vscode-extension/pbir-design-analyzer-0.6.0-linux-x64.vsix`
  - `vscode-extension/pbir-design-analyzer-0.6.0-darwin-x64.vsix`
  - `vscode-extension/pbir-design-analyzer-0.6.0-darwin-arm64.vsix`

## Residual Notes

- Packaging completed cleanly in this pass.
- Historical `0.5.0` references in changelog sections were intentionally left unchanged.
