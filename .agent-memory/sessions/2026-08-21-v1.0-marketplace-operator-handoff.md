# Session — 2026-08-21 v1.0 Marketplace operator handoff

## Objective

Complete v1.0 release administration only: verify release inputs, hand off manual Marketplace publication, and close the consolidation plan only after independent Marketplace verification.

## Findings

- `v1.0.0` annotated tag peels to `4c56eaf37f4829640051ec121d9f6f5103aa7084`.
- GitHub Release `374710589` is published as PBIR Design Analyzer v1.0.0 from `v1.0.0`; release workflow run `32534183564` created five uploaded VSIX assets.
- Downloaded GitHub Release assets independently match `docs/release-evidence/v1.0.0-release-assets.json` SHA-256 values. Each package contains product `pbir-design-analyzer`, display name PBIR Design Analyzer, publisher `bcrowell`, version `1.0.0`, and the expected target metadata.
- Public Marketplace API confirms the existing listing `bcrowell.pbir-design-analyzer` remains at `0.7.0`; version `1.0.0` is not published.
- Main ruleset `21156915` and tag ruleset `21156927` are active with zero bypass actors.

## Decision

Marketplace publication requires the authorized human operator. No credential was requested, created, stored, or exposed. The consolidation plan remains open; no release evidence was rewritten as complete and no post-v1.0 work was started.

## Exact handoff

Upload the five validated v1.0.0 VSIX assets from GitHub Release 374710589 to the existing `bcrowell.pbir-design-analyzer` Marketplace listing, one at a time, preserving identity and metadata. Stop on any rejection or apparent replacement behavior. After completion, report the portal result for independent public verification.
