# PBIR Design Analyzer v1.0 Readiness Report

## Current validation decision — 2026-08-21

### Executive decision

**READY WITH DOCUMENTED LIMITATION — DO NOT TAG OR PUBLISH FROM THIS VALIDATION BRANCH**

Repository-controlled characterization, contract, packaged deterministic workflow, installed-host, and GitHub governance gates pass. Hosted Windows x64 acceptance now proves native packaged execution; darwin-arm64 has native proof; darwin-x64 has Rosetta proof; linux-x64 has x86_64 container proof. win32-arm64 remains package-only because no hosted Windows ARM64 runner is configured. The hosted macOS Intel leg was queued on macos-13 and canceled after the required Windows, Linux, and macOS arm64 acceptance legs completed; its local Rosetta proof remains valid.

### Repository identity

| Field | Value |
|---|---|
| Branch | `codex/hosted-v1-readiness-validation-2026-08-21` |
| Commit | `8d652c4c36b0e6a351c33f846aa2f241223d2148` |
| Extension version | `0.7.0` |
| Validation timestamp | `2026-08-21` (America/New_York) |
| Worktree | Dirty; pre-existing stabilization edits and this session's release-control changes are preserved. No tag, release, Marketplace publication, or GitHub settings change was made. |

### Gate matrix

| Gate | Result | Evidence |
|---|---|---|
| Backend | PASS | Release run: **1,034 passed**, **11 expected Windows skips**, 0 failed. Prior baseline: 1,033 passed, 11 skips; +1 representative corpus test. |
| Extension Jest | PASS | 100 suites / **532 passed**; unchanged from prior baseline. |
| Webview Jest | PASS | 11 suites / **68 passed**; unchanged from prior baseline. |
| TypeScript | PASS | `npm run compile`. |
| ESLint | PASS | `npm run lint`, zero errors. |
| Architecture | PASS | Existing architecture suite/source guards remain passing. |
| Contract generation/freshness | PASS | `npm run validate:contract`; generated C# and TypeScript output matches `contracts/score-panel/v1/schema.json`. |
| Compatibility fixtures | PASS | **6** fixtures: valid, additive unknown, nullable mutation, missing required, incompatible version, invalid mutation. |
| Characterization corpus | PASS | Original minimal golden plus **5 representative synthetic fixtures**; manifest and compact goldens are read-only. |
| Deterministic repeat | PASS | Corpus command runs the characterization filter twice; identical normalized fingerprints. |
| Packaged backend | PASS | Hosted win32-x64 native plus darwin-arm64 native, darwin-x64 Rosetta, and linux-x64 x86_64-container initialize/ping/score passed and repeated deterministically. |
| Packaged workflow | PASS | Hosted win32-x64, darwin-arm64 native, darwin-x64 Rosetta, and linux-x64 container packaged workflows passed generation/import/score/preview/apply/rollback/export. |
| Native packaged runner matrix | PASS WITH DOCUMENTED LIMITATION | Hosted Linux x64, Windows x64, and macOS arm64 legs passed in run 32511720555. macOS x64 was queued on macos-13 and canceled; local Rosetta proof passed. Windows ARM64 remains package-only. |
| Installed VS Code host | PASS WITH DOCUMENTED LIMITATION | darwin-arm64 VSIX installed into isolated directories, activated in a disposable VS Code host, and resolved its packaged backend; other target hosts were not available locally. Evidence: `packaged-host-acceptance-darwin-arm64.json`. |
| Five-target packaging | PASS | `npm run package:all`; win32-x64, win32-arm64, linux-x64, darwin-x64, darwin-arm64 rebuilt. |
| Package contents | PASS | All five `verify:vsix` checks passed; entry counts 52, 233, 52, 52, 52 respectively. |
| Target/version contract | PASS | 5 targets at version 0.7.0. |
| Workflow YAML | PASS | CI, release, and security workflow YAML parsed successfully. |
| Dependency/security policy | PASS WITH DOCUMENTED LIMITATION | 16 npm advisories classified; high findings have explicit owner/expiry exceptions; NuGet high transitive findings are recorded. CodeQL workflow is configured but hosted execution is not locally reproduced. |
| Artifact manifest | PASS WITH DOCUMENTED LIMITATION | `docs/release-evidence/artifact-manifest.json` records commit, version, RID, package/backend hashes, tool versions; signed attestation/SBOM are not configured and remain optional under current policy. |
| Documentation validation | PASS | Current README links, release guide, five-target matrix, scope, and readiness report validated. |
| Local hooks | PASS WITH DOCUMENTED LIMITATION | Existing hooks remain convenience controls, not authoritative security boundaries. |
| GitHub repository rules | PASS | Active rulesets 21156915 and 21156927 verified at 2026-08-21 13:05:09 -04:00; the legacy branch-protection endpoint remains 404 because governance is implemented through repository rulesets. |
| v1.0 workflow acceptance | PASS | Complete deterministic packaged workflow proof passed in hosted Windows native, macOS native, macOS Rosetta, and Linux x86_64-container modes. Installed-host activation passed for darwin-arm64. |
| Repository hygiene | PASS | `git diff --check` passed; unrelated dirty files were preserved. |

### Characterization baseline

- Fixture count: **5 representative structural fixtures**, plus the retained original minimal golden test.
- IDs/categories: `characterization-minimal` (standard), `characterization-multipage` (page ordering), `characterization-hidden-navigation` (hidden/navigation metadata), `characterization-custom-visual` (Deneb/HTML-equivalent metadata), and `characterization-malformed` (unsupported input diagnostics).
- Golden count: **6** expected snapshots total; no command overwrites them.
- Manifest: `service-dotnet/tests/Fixtures/Characterization/manifest.json`, with synthetic provenance, privacy status, input hashes, classification, and expected surfaces.
- Negative proof: changing the multi-page expected composite from 74.25 to 74.26 failed the clean characterization gate; the value was restored. Contract freshness was likewise proven to fail after a disposable generated-file edit.
- Golden approval: `scripts/approve-pbir-golden-change.mjs <fixture-id> <reason> <reviewer/reference>` records auditable approval metadata and never changes expected output.
- Deferred categories: larger performance corpus and a structurally distinct Fabric App fixture. Bookmark/interaction metadata is represented in the hidden-navigation synthetic fixture; packaged mutation/export behavior is now covered on darwin-arm64 by the disposable generated workflow.

### Packaged acceptance baseline

| Target | Package SHA-256 | Backend SHA-256 | Package verification | Runtime proof | Deterministic proof | Layer |
|---|---|---|---|---|---|---|
| win32-x64 | `0b5b9e4bf842691b5de2f09ebf426ae1870f15abf33acd80ba86b7587d42d0bd` | `fa2b614b2a0c0bf625618fe4741627db4b4be16748226cd39b4dcb2cca1d8c1a` | PASS | hosted Windows Server 2025 native | repeated fingerprint `9d3393ff…`; mutation/rollback/export PASS | packaged-workflow/native |
| win32-arm64 | `d92295a597258cd5c592b7018d47f1f196e5fd24911fd76787318392e58b4e59` | `44b23452214d40a15519f7c2386dbe3d70c203fd26a9c298f4a9ca2d96352583` | PASS | unavailable locally | package-only | package-only |
| linux-x64 | `14e1b24765869e8005012d1bda9633941fc52dfa2920254211d532b35ce46579` | `8d72d17ae7f652faa266f4378e0390cf64f3909647cb643b57c0476503071616` | PASS | x86_64 container | repeated fingerprint `9d3393ff…` | packaged-workflow/container |
| darwin-x64 | `55d7ba10661a8820f786e448bcbca4304e5a85c829b33217bcf2c42215b59e6e` | `fff6879f0f5fa933f68505311f9f76f71e14944a562f2be9a2a8d242701cfaca` | PASS | Rosetta x86_64 | repeated fingerprint `9d3393ff…` | packaged-workflow/rosetta |
| darwin-arm64 | `a15e26dff17e37f3ff10eacd74daa2138793f652b0eea78ca1bc47c94c25ca5b` | `e065245536dc4bbbc6bff89cbf3735aa6202703ff419a5ee93d03da2104c4ed5` | PASS | initialize/ping/score/generate/import/preview/apply/rollback/export PASS | repeated normalized fingerprint `9d3393ff372deb045ee228dabefa31053a8119ff9e4ae0ddddd66d3209b7c9a9` | packaged-workflow |

Full hashes and lineage are in `docs/release-evidence/artifact-manifest.json` and per-target packaged acceptance JSON. The hosted Windows evidence is retained from CI run `32511720555`, job `96864952609`, at commit `8d652c4c36b0e6a351c33f846aa2f241223d2148`.

Installed-host baseline: darwin-arm64 VSIX installation and activation passed in an isolated VS Code host; the packaged backend entrypoint was present and the extension started its analyzer backend. Other target host environments were not available locally.

### Contract baseline

- Authority: `contracts/score-panel/v1/schema.json`.
- Generated surfaces: `vscode-extension/src/generated/scorePanelContract.ts` and `service-dotnet/RpcHost/GeneratedScorePanelContract.g.cs`.
- Runtime TypeScript score-panel guards remain in place; the generated constants now supply the protocol/schema versions.
- Compatibility fixture count: **6**. Unknown additive fields are tolerated; required envelope/state/mutation fields, nullable transaction IDs, enum operations, and protocol version are checked.
- Freshness: clean generation plus `--check` passes.
- Manual surface remaining: the full legacy ScoreResult DTO/projection remains manually maintained beyond the stable representative contract subset; existing runtime guards protect it.

### Security, artifact, and documentation baseline

- npm audit: 16 advisories classified; high/critical findings fail unless explicitly owned, scoped, and unexpired. Current exceptions expire 2026-12-31 in `config/security-baseline.json`.
- NuGet audit: two high transitive test-graph findings classified under the same expiring baseline.
- CodeQL and secret-scanning policy are represented in `.github/workflows/security.yml`; hosted execution is not claimed from local evidence.
- Artifact manifest and SHA-256 package/backend hashes pass. SBOM and signed provenance are not configured and are optional under the current release policy.
- Documentation validator passes current links, release authority, target matrix, scope, and version facts without rewriting historical documents.

### Architecture baseline

Existing composition-root and source-level architecture controls remain passing. Scoring remains authoritative in backend `ScoreResult`; normalized findings and presentation projections remain downstream. Dormant/provider architecture was not deleted or registered, and no scoring refactor was introduced during corpus establishment.

### GitHub administrator-control verification

**PASS — verified 2026-08-21 13:05:09 -04:00.** Repository rulesets are active and enforce the approved server-side release controls.

| Ruleset | ID | Target | Enforcement | Verified controls |
|---|---:|---|---|---|
| `main-production-protection` | `21156915` | `refs/heads/main` | `active` | Pull request required; one approval; stale review dismissal; review-thread resolution; strict up-to-date branch policy; required checks `build-test (ubuntu-latest)`, `build-test (windows-latest)`, `build-test (macos-latest)`, and `package-targets`; deletion and force-push blocked. |
| `release-tag-protection` | `21156927` | `refs/tags/v*` | `active` | Version-tag deletion and non-fast-forward updates blocked; tag creation remains available to the release workflow so new version tags can be created. |

Both rulesets have no bypass actors. The legacy branch-protection API still reports `main` as unprotected; this is expected because the repository uses rulesets rather than the legacy protection model. No application files or product scope were changed.

### Scope freeze

The frozen classification in `docs/product/scope.md` remains intact: deterministic PBIR review workflow is **Core**; Rendered Review and Fabric App Review are **Optional/advisory**; dormant provider/runtime architecture is **Experimental**; Visual Intelligence, broad generation/deployment, provider execution, and enterprise governance expansion are **Deferred**; autonomous deployed-app inspection, AI-controlled mutation, and publication from this review are **Unsupported**. No scope correction was required.

### Known limitations and release decision

The v1.0 validation gates pass with two explicit limitations: Windows ARM64 is package-only, and the hosted macOS Intel leg was canceled after macos-13 remained queued; local Rosetta acceptance passed. This validation branch must not be tagged or published. Before release administration, merge through the protected PR path and repeat the release workflow from the intended release commit; do not treat this validation PR as the v1.0 release source.

## Governance resolution — solo-maintainer exception — 2026-08-21

The repository is intentionally operated as a solo-maintainer project. `bulova23` is the single authorized maintainer and no independent reviewer with repository write access is currently available. Requiring an independent approval would make the protected merge path impossible for this repository.

The repository owner therefore approved the minimum governance exception: `main-production-protection` ruleset `21156915` changed its required approving review count from **1** to **0**. This removes only the impossible approval requirement; it does not disable pull requests or authorize bypassing the ruleset.

The following compensating controls remain active and are authoritative:

- pull request required for changes to `main`
- all eight required build/package status contexts
- strict up-to-date branch policy
- stale-review dismissal and review-conversation resolution
- force-update and branch-deletion protection
- active `release-tag-protection` ruleset `21156927` for `v*` tags, blocking deletion and non-fast-forward movement
- zero bypass actors on both rulesets
- release evidence, post-merge validation, and the controlled release checklist

The approval policy must be revisited when an additional maintainer or contributor with formal review authority is added. At that point, the repository should restore a required independent approval through a separately reviewed governance change.

Live verification after the exception: ruleset `21156915` is active with `required_approving_review_count: 0`, all eight required checks, and no bypass actors; ruleset `21156927` remains active with tag deletion and non-fast-forward protection. This section supersedes the earlier one-approval snapshot above while preserving that historical evidence.

## Post-merge validation — 2026-08-21

PR #6 was merged through the protected pull-request workflow at `2026-08-21T20:05:34Z`.

| Field | Value |
|---|---|
| PR | #6 |
| Merge commit | `7389c63fced90eff3008d2a918bf2212354178f1` |
| Resulting protected main | `7389c63fced90eff3008d2a918bf2212354178f1` |
| CI run | [32521799882](https://github.com/bulova23/pbir-design-analyzer/actions/runs/32521799882) |
| Security run | [32521799883](https://github.com/bulova23/pbir-design-analyzer/actions/runs/32521799883) |
| Version during validation | `0.7.0` |

Post-merge hosted validation passed on the merged main commit:

- backend build/test: 1,034 passed, 11 expected Windows skips
- extension Jest: 532 passed
- webview Jest: 68 passed
- Ubuntu, Windows, and macOS build/test jobs
- release-contract and release-gates
- all five package-target matrix jobs
- packaged runtime acceptance for Linux x64, Windows x64, and macOS arm64
- dependency policy and CodeQL for C# and TypeScript

Merged-main local validation independently passed TypeScript compilation, ESLint, six contract compatibility fixtures and freshness, release-target validation, documentation validation, characterization repeat 2/2, security policy, five-target packaging, and VSIX content verification. The regenerated artifact manifest records the merged source commit, version, target package hashes, backend hashes, tool versions, fixture identity, and the current SBOM/provenance limitations.

The workflow change remains valid: hosted macOS Intel acceptance is removed because the `macos-13` runner could remain queued indefinitely; darwin-x64 remains covered by the retained local Rosetta packaged-workflow evidence. Windows ARM64 remains package-only. No version tag, GitHub Release, or Marketplace publication was created.

## Version alignment preparation

Post-merge validation passed, so version alignment proceeds through a separate protected branch and pull request. The current extension and backend release metadata remains `0.7.0` during this validation record; the version-alignment PR targets `1.0.0` and must pass its own required checks before any tag or publication action.

## Prior readiness decision preserved

The following historical record is retained from the preceding stabilization review; it is not the current decision.

## Executive decision

**NOT READY**

The current repository-controlled regression suites, target packaging, release/version contract, lint, architecture guards, and basic golden check pass. Readiness cannot be approved because v1.0 acceptance controls are incomplete: characterization is a single minimal fixture, packaged behavioral acceptance is not automated, cross-language validation is literal-presence freshness rather than compatibility enforcement, and security/dependency/SBOM/provenance/documentation gates are not implemented. GitHub administrator protections are also absent, but they are not the only blocker.

## Repository identity

| Field | Value |
|---|---|
| Branch | `main` |
| Commit | `a67af7ad88bf2e7d6fd4bc162be84731d5ae1390` |
| Version | `0.7.0` |
| Validation time | 2026-08-21T14:05:01Z |
| Dirty state | Dirty; stabilization changes, release evidence, scope, hooks, fixtures, and unrelated existing work are present |
| Preserved unrelated work | Existing dirty product/source/docs files were not reset or rewritten |

## Tasks 1–8 reconciliation

| Task | Classification | Evidence / limitation |
|---|---|---|
| 1. Release pipeline recovery | Complete with documented limitation | Portability fixes and CI matrix changes are present; local Release tests pass. A fresh hosted CI rerun is not available in this session. |
| 2. Release/version/target contract | Complete with documented limitation | `config/release-targets.json` drives five target validation and packaging. Tag validation is workflow-level; the manifest does not contain every proposed descriptive field. |
| 3. Characterization/golden gates | Partially complete | `characterization-minimal` golden passes repeatedly. The planned representative corpus, extension characterization command, approval/change manifest, and perturbation proof are absent. |
| 4. Packaged-product proof | Partially complete | All five VSIXes build and pass content verification. Packaged RPC handshake/score/mutation/export and installed VS Code host acceptance are not automated. |
| 5. Architecture boundaries | Complete with documented limitation | Seven architecture tests plus source-level composition/scoring guards pass. Enforcement is source/namespace-level, not assembly isolation or a generated dependency graph. |
| 6. Cross-language contracts | Partially complete | Three contract checks pass, but they check required literals only. No generated schema/types, compatibility fixtures, additive-field tests, or freshness diff gate is present. |
| 7. Security/artifact/documentation gates | Partially complete | Lint, version/target and package checks exist. Dependency policy, CodeQL/secret scanning, SBOM, provenance, artifact manifest, and general documentation validators are absent. |
| 8. Hooks/GitHub rules | Partially complete | Hooks are checked in and pre-commit is fast. Pre-push runs full suites rather than an affected moderate subset; GitHub protections are absent. |

## Gate matrix

| Gate | Result | Evidence |
|---|---|---|
| Backend Release build/test | PASS WITH DOCUMENTED LIMITATION | `dotnet test ... -c Release`: 1,033 passed, 11 expected Windows-only skips, 0 failed. macOS cannot execute Windows-native integration tests. |
| Extension Jest | PASS | 100 suites, 532 tests passed. |
| Webview Jest | PASS | 11 suites, 68 tests passed. |
| ESLint | PASS | `npm run lint`, zero errors. |
| TypeScript | PASS | `npm run compile`. |
| Architecture | PASS | Architecture filter: 7 passed; source guards cover composition root and scoring. |
| Contract | PASS WITH DOCUMENTED LIMITATION | Three protocol freshness checks pass; they do not mechanically validate the full C#/TypeScript schema. |
| Characterization/goldens | PASS WITH DOCUMENTED LIMITATION | One sanitized minimal fixture golden passed; corpus breadth is insufficient for v1.0. |
| Deterministic repeat | PASS WITH DOCUMENTED LIMITATION | Same minimal fixture produced identical golden output in repeated process runs; no multi-fixture/cross-target comparison exists. |
| Packaging | PASS | `npm run package:all` generated all five manifest targets. |
| Package contents | PASS WITH DOCUMENTED LIMITATION | Four packages contain 52 entries; Windows ARM64 self-contained package contains 233 entries. All pass `verify:vsix`; no packaged RPC behavioral smoke was run. |
| Target matrix | PASS | win32-x64, win32-arm64, linux-x64, darwin-x64, darwin-arm64. |
| Version contract | PASS | Version `0.7.0` synchronized and release contract validator passes. |
| Workflow parsing | PASS | Ruby YAML parsing passed for CI and release workflows. |
| Documentation validation | NOT RUN | No general documentation validator is implemented; release contract checks selected target facts only. |
| Security/dependency controls | NOT RUN | No enforced audit policy, CodeQL, secret scan, SBOM, or provenance gate is present. |
| Local hooks | PASS WITH DOCUMENTED LIMITATION | Pre-commit is fast and scoped; pre-push is broader than the approved affected/moderate design. Disposable failure injection was not run against the dirty worktree. |
| GitHub repository rules | ADMINISTRATOR ACTION REQUIRED | Read-only GitHub API: `main` is not protected and no rulesets were returned. |
| Release artifact proof | PASS WITH DOCUMENTED LIMITATION | SHA-256 hashes generated for all five VSIXes; no signed provenance/SBOM/artifact manifest. |
| v1.0 workflow acceptance | FAIL | Targeted source workflow tests passed (48), but no complete packaged/installed-host Discover → Score → Evidence → Prioritize → Preview → Apply → Rollback → Deliver run was proven. |
| Repository hygiene | PASS | `git diff --check` passed; generated tracked backend binaries were not intentionally retained as readiness changes. |

## Supported target matrix

| Target | VSIX | Validation | SHA-256 |
|---|---|---|---|
| win32-x64 | `pbir-design-analyzer-0.7.0-win32-x64.vsix` | PASS, 52 entries | `0619ba0dfd5ecdfeac294cbeb546058dcae6eee275568ab7000e5ce86c36de73` |
| win32-arm64 | `pbir-design-analyzer-0.7.0-win32-arm64.vsix` | PASS, 233 entries, self-contained | `c985a7c3c1dfd54d4df3083d9794a489bd356ec7c93ed352af7aa51e8bed461f` |
| linux-x64 | `pbir-design-analyzer-0.7.0-linux-x64.vsix` | PASS, 52 entries | `179eb92a42b0ca5a5c2f2eafcfbfa51e82db43ee9740d226af5308c4e60a53ff` |
| darwin-x64 | `pbir-design-analyzer-0.7.0-darwin-x64.vsix` | PASS, 52 entries | `a3e5fb763d853d03b5a02ea3e487eb50fd4edd2cf9ee4a878832175a1443095f` |
| darwin-arm64 | `pbir-design-analyzer-0.7.0-darwin-arm64.vsix` | PASS, 52 entries | `9655c382174527d499e01d0205b6b2521b94e39c38ea293b3704697a1b4d7cdb` |

## Behavioral baseline

- Fixture: `service-dotnet/tests/Fixtures/Characterization/MinimalReport.Report`.
- Golden: `characterization-minimal.golden.json`, `score-characterization/v1`.
- Captures page/visual counts, category/composite scores, feedback, recommendations, diagnostics, evidence, readiness, and deterministic fingerprint.
- The golden passed repeatedly and was not updated.
- Targeted mutation/materialization/export source tests: 48 passed.
- Packaged/installed-host mutation, rollback, and export evidence: not run.

## Contract baseline

- Protocol/version literals: score-panel envelope v1 and two RPC contract families; validator reports three contracts passed.
- Authority is manually maintained C# and TypeScript source plus `protocol-contracts.json` required-literal checks.
- Unknown/additive fields, required-field compatibility, supported-version negotiation, and generated freshness are not comprehensively enforced.

## Architecture baseline

- `RpcHost/Program.cs` registers scoring and dispatcher services without Phase35/provider/authoring infrastructure.
- Scoring source does not reference presentation or experimental infrastructure under current source guards.
- Architecture tests remain source-level; no generated dependency graph or assembly boundary has been added.
- Dormant provider/runtime/generation code remains compiled in broader backend projects but is not registered in the shipped analyzer composition root.

## Administrative handoff

The exact handoff is also recorded in `2026-08-21-ci-failure-classification.md`. An administrator must configure and verify:

1. PR-only merges to `main`; direct pushes prohibited.
2. Required checks: `build-test (ubuntu-latest)`, `build-test (windows-latest)`, `build-test (macos-latest)`, and `package-targets`.
3. Up-to-date branches required; force-push and branch deletion prohibited.
4. Resolved review conversations required; administrator bypass retained only for documented emergency use.
5. `v*.*.*` release tags protected from ordinary update/deletion.

Current status: **ADMINISTRATOR ACTION REQUIRED — NOT VERIFIED**. The read-only GitHub API reports `main` is not protected and returns no rulesets.

## Known limitations

- Hosted CI has not been rerun from this session after the portability fixes.
- Characterization coverage is one minimal sanitized fixture, not the planned representative corpus.
- No installed VS Code Extension Host acceptance or packaged RPC end-to-end score/mutation/export proof was completed.
- Security/dependency, SBOM, provenance, and general documentation gates are absent.
- Protocol enforcement does not yet eliminate material manual C#/TypeScript contract synchronization.
- Windows-only integration skips are expected on macOS; they require Windows CI confirmation.

## Scope freeze

The authoritative classification is [docs/product/scope.md](../product/scope.md). Core remains the deterministic PBIR workflow. Rendered Review and Fabric App advisory review remain optional. Visual Intelligence, broad generation/deployment, enterprise governance expansion, provider execution, and autonomous/deployed-app behavior remain deferred or unsupported.

## Release decision

The earliest failing technical phase is Task 3/4/6/7 acceptance, not GitHub administration. Do not tag or publish v1.0. Complete the representative characterization corpus, packaged behavioral acceptance, mechanical contract compatibility/freshness enforcement, and required security/artifact/documentation gates; then rerun hosted CI and administrator protection verification.
